<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyCitroenC5X7" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/background_dark</item>
<!--        <item name="android:statusBarColor">false</item>-->
        <!-- Customize your theme here. -->
    </style>

    <style name="CustomSwitchStyle" parent="Widget.MaterialComponents.CompoundButton.Switch">
        <!-- Размер ползунка (thumb) -->
        <item name="thumbWidth">40dp</item>
        <item name="thumbHeight">24dp</item>

        <!-- Размер фона (track) -->
        <item name="trackHeight">32dp</item>

        <!-- Внутренние отступы -->
        <item name="android:padding">8dp</item>
    </style>
    <style name="BottomNavTextAppearanceActive" parent="TextAppearance.MaterialComponents.Body1">
        <item name="android:textSize">16sp</item>
        <item name="android:textAlignment">center</item>
        <item name="android:padding">50dp</item>
    </style>

    <style name="BottomNavTextAppearanceInactive" parent="TextAppearance.MaterialComponents.Caption">
        <item name="android:textSize">14sp</item>
    </style>
</resources>